/** @type {import('next').NextConfig} */
 
export const experimental = {
    serverActions: {
        allowedOrigins: ['https://eisomoy-dashboard-node.vercel.app', 'https://ei-matro.vercel.app'],
    },
};